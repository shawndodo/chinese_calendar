require "spec_helper"

describe ChineseCalendar do
  it "has a version number" do
    expect(ChineseCalendar::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
